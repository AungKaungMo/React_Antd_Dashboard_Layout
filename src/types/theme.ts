export type Theme = "blue" | "purple" | "green" | "yellow";

export type Mode = "light" | "dark";

export type Current_Theme = {
  color_primary: string;
  color_bg: string;
  background_text: string;
  mute_background: string;
  mute_color_text: string;
  mute_color_text_secondary: string;
  color_text: string;
};
